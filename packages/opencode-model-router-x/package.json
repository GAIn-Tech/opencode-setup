{
  "name": "@jackoatmon/opencode-model-router-x",
  "version": "0.1.0",
  "description": "Policy-based model router with live outcome tuning for OpenCode",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./policies": "./src/policies.json"
  },
  "scripts": {
    "test": "node -e \"const { ModelRouter } = require('./src/index.js'); const r = new ModelRouter(); const pick = r.route({ complexity: 'high' }); console.log('route:', pick); r.recordResult(pick.modelId, true, 1200); console.log('models:', r.listModels().map(m => m.id)); console.log('ALL TESTS PASSED');\""
  },
  "keywords": [
    "opencode",
    "model-router",
    "llm",
    "routing",
    "policy"
  ],
  "author": "OpenCode",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
