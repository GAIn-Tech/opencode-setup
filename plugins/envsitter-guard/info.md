# envsitter-guard

Prevents reading .env files — secrets protection.

- **npm**: `envsitter-guard@latest`
- **Purpose**: Blocks the agent from reading .env, credentials, and secret files
- **Complements**: `cc-safety-net` (which blocks destructive commands — this blocks secret exposure)
