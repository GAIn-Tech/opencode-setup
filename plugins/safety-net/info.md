# cc-safety-net

Blocks destructive git and filesystem commands before they execute.

- **Package**: `cc-safety-net@latest`
- **Source**: npm registry
- **Purpose**: Prevents accidental `rm -rf`, `git push --force`, `git reset --hard`, and other destructive operations
